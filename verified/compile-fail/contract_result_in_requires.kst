// `result` names the returned value and exists only inside `@ensures` — a
// precondition is checked before the function runs, so there is no result yet.
@requires(result > 0)
func f(int32 n) -> int32 {
    return n
}
func main() -> void {
    println("{f(1)}")
}
